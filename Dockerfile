#Docker公式ドキュメントに解説あり

# 元にするベースイメージの取得
FROM python:3.12.11-slim

# Dockerfileコマンドを実行する作業ディレクトリを指定
WORKDIR /app

# イメージにファイルやフォルダを追加
COPY 

# イメージをビルドする時にコマンドを実行
RUN

# コンテナが通信を待ち受けるポートを指定
EXPOSE 5000

# コンテナを起動するときに実行するコマンドを指定
CMD 